# ==========================================
# Minimal .dockerignore for "Fat Container"
# ==========================================
# Philosophy: Copy most things (node_modules, .yarn cache, builds)
# Only exclude: Git, logs, local env files
# ==========================================

# Git (never needed in container)
.git
.github
.gitignore
.gitmodules
.gitattributes

# Local environment files (use Railway env vars instead)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Logs (don't copy old logs)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# IDE files
.vscode
.idea
*.swp
*.swo
*~
.cursor

# Documentation (save space)
*.md
!README.md
docs

# Test results (not needed in production)
coverage
.nyc_output
playwright-report
test-results

# Docker itself
Dockerfile*
docker-compose*.yml
.dockerignore

# Platform-specific
.vercel
.turbo
fly.toml
.fly

# Build artifacts that will be regenerated (but keep .next itself!)
.next/cache
# Note: apps/web/.next/ is INCLUDED so Docker can use existing build

# ==================================================
# KEEP THESE (don't add to .dockerignore):
# ==================================================
# ✅ node_modules/ - Already installed, reuse them
# ✅ .yarn/ - Cache makes installs faster
# ✅ packages/ - Already built, reuse them
# ✅ apps/web/.next/ - Already built, reuse it
# ✅ Test files - TypeScript excluded via tsconfig
# ==================================================
